## 1. Project Setup

- [ ] 1.1 Initialize package.json with dependencies (express, uuid) and devDependencies (typescript, tsx, @types/*)
- [ ] 1.2 Create tsconfig.json with strict mode enabled and ESM module settings
- [ ] 1.3 Create src/ directory structure (types, validators, services, routes, middleware, __tests__)

## 2. Type Definitions

- [ ] 2.1 Define TaskStatus, TaskPriority, Task types in src/types/task.ts
- [ ] 2.2 Define CreateTaskInput, UpdateTaskInput types with strict field constraints
- [ ] 2.3 Define ApiResponse<T> and PaginatedResponse<T> envelope types
- [ ] 2.4 Define AppError, ValidationError, NotFoundError classes

## 3. Input Validation

- [ ] 3.1 Implement validateCreateTask(input: unknown): CreateTaskInput in src/validators/task.ts
- [ ] 3.2 Implement validateUpdateTask(input: unknown): UpdateTaskInput with at-least-one-field check
- [ ] 3.3 Implement validateStatusTransition(current: TaskStatus, next: TaskStatus): void with state machine rules

## 4. Service Layer (Pure Functions)

- [ ] 4.1 Implement createTask(store, input): Task — generate UUID, set defaults, add to store, return new task
- [ ] 4.2 Implement listTasks(store, filter?): Task[] — filter by status, apply offset/limit pagination
- [ ] 4.3 Implement getTask(store, id): Task — find by ID, throw NotFoundError if missing
- [ ] 4.4 Implement updateTask(store, id, input): Task — validate status transition, apply changes immutably
- [ ] 4.5 Implement deleteTask(store, id): void — remove from store, throw NotFoundError if missing
- [ ] 4.6 Implement createTaskStore(): Map<string, Task> — factory for the in-memory store

## 5. Route Handlers

- [ ] 5.1 Implement POST /api/v1/tasks handler with validation → service → response
- [ ] 5.2 Implement GET /api/v1/tasks handler with query param parsing and pagination
- [ ] 5.3 Implement GET /api/v1/tasks/:id handler
- [ ] 5.4 Implement PATCH /api/v1/tasks/:id handler
- [ ] 5.5 Implement DELETE /api/v1/tasks/:id handler

## 6. Middleware

- [ ] 6.1 Implement global error handling middleware that maps AppError subtypes to HTTP status codes

## 7. Application Entry

- [ ] 7.1 Create src/app.ts — configure Express with JSON parsing, routes, error middleware
- [ ] 7.2 Create src/index.ts — start server on configurable port, log startup message

## 8. Integration Tests

- [ ] 8.1 Write test: create task with valid input → 201 with Task object
- [ ] 8.2 Write test: create task with empty title → 400
- [ ] 8.3 Write test: list tasks → 200 with paginated response
- [ ] 8.4 Write test: list tasks with status filter → 200 with filtered results
- [ ] 8.5 Write test: get task by ID → 200; get non-existent → 404
- [ ] 8.6 Write test: update task title → 200; invalid status transition → 400
- [ ] 8.7 Write test: delete task → 204; get deleted → 404

## 9. Manual Verification

- [ ] 9.1 Start server and run curl commands for all 5 endpoints
- [ ] 9.2 Verify status state machine transitions with curl
- [ ] 9.3 Verify error response envelope format for 400 and 404 cases
